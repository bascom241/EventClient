import React from 'react';
import { StyleSheet, View } from 'react-native';
import SetLocation from '@/features/profile/components/SetLocation';
const Location = () => {
    return (
        <SetLocation />
    );
}

const styles = StyleSheet.create({})

export default Location;
